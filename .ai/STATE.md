# STATE

## 현재 작업 (2026-08-13)

- 작업명: 역할별 메인페이지 개선 및 비로그인 리뷰 API 연동
- 관련 Issue: 확인 필요
- 관련 브랜치: 현재 작업 브랜치
- 작업 목적: 클라이언트·프리랜서 메인에 로그인 사용자 이름을 표시하고, 데스크톱 화면 크기를 확대하며, 비로그인 메인의 더미 리뷰를 공개 리뷰 API로 교체합니다.

## 이번 작업 범위

- `GET /api/v1/home/site-reviews?size=6` 공개 API 연동
- 리뷰 API 실패·빈 배열 시 리뷰 섹션 내 안내 문구 표시 및 nullable 필드 처리
- `GET /api/v1/auth/me` 응답의 로그인 사용자 이름을 역할별 메인 히어로에 표시
- 클라이언트·프리랜서 메인의 콘텐츠 폭, 글자, 카드, 버튼, 표 크기 확대
- lint, build, 관련 테스트 및 정적 diff 검증

## 진행 상황

- [x] API 명세와 기존 메인·인증 구조 확인
- [x] 구현
- [x] 정적 검증

## 미검증·확인 필요

- 실제 공개 리뷰 API 응답과 빈 배열·500 응답은 미검증
- 로그인 테스트 계정이 없어 역할별 실제 이름과 반응형 화면은 브라우저에서 미검증
- `npm run lint`: 오류 0건, 기존 `ProfileMenu.tsx` 미사용 prop 경고 1건
- `npx tsc --noEmit`: 통과
- `npm run build`: 통과
- `npm run test -- --runInBand`: 12 suites/56 tests 통과, 기존 프리랜서 마이페이지 테스트 1개 실패 — 테스트는 `변경하기` 링크를 기대하지만 현재 구현은 인라인 폼을 여는 버튼
- `git diff --check`: 통과

---

## 현재 작업 (2026-08-13)
이 문서는 현재 AI와 함께 진행 중인 한 가지 작업만 기록합니다.

## 현재 작업

- 작업명: 고객지원 1:1 문의 단위·컴포넌트 테스트 작성
- 작업명: SVG 시안 기반 클라이언트 리뷰 작성 화면 구현
- 관련 Issue: 확인 필요
- 관련 브랜치: 현재 작업 브랜치
- 작업 목적: 전달받은 기본·입력 상태 시안을 클라이언트 리뷰 작성 경로에 동일하게 반영합니다.

- 작업명: 프리랜서 기본 정보 내 비밀번호 변경 폼 확장
- 작업 목적: 비밀번호 변경 클릭 시 별도 페이지로 이동하지 않고 기본 정보 화면의 기존 콘텐츠를 유지한 채 변경 폼을 추가 표시
- 진행 상태: 구현 및 정적 검증 완료
- 검증: `npm run lint`, `npm run build`, `git diff --check` 통과
- 미검증: 로그인된 브라우저 세션에서 실제 화면 비교
- 확인 필요: 실제 이메일 인증과 비밀번호 변경은 기존 API 동작에 의존

---

이 문서는 현재 AI와 함께 진행 중인 한 가지 작업만 기록합니다.

## 현재 작업

- 작업명: 클라이언트·프리랜서 마이페이지 입력창 호버 스타일 통일 및 프리랜서 마이페이지 배치 점검
- 관련 Issue: 확인 필요
- 관련 브랜치: 현재 작업 브랜치
- 작업 목적: 입력창 호버 시 남색(브랜드) 테두리가 모든 마이페이지 화면에서 일관되게 나타나도록 하고, 프리랜서 마이페이지의 배치 불일치를 코드 검토로 찾아 보정합니다.

## 이번 작업 범위

- 클라이언트·프리랜서 마이페이지의 `input`/`select` 호버 스타일을 기존에 이미 적용된 `fieldClassName` 패턴 기준으로 통일
- 프리랜서 마이페이지 placeholder 카드 padding을 다른 화면과 동일하게 보정
- lint, build (로그인 세션이 없어 실제 화면 확인은 미실행)

## 진행 상황

- [x] 클라이언트·프리랜서 마이페이지의 input/select 스타일 전체 조사
- [x] 호버 시 남색 테두리가 없던 항목 보정 (스킬 숙련도 select, 기업정보 입력, 결제수단 입력, 비밀번호 변경 입력)
- [x] 프리랜서 마이페이지 placeholder 카드 padding 불일치 보정
- [x] `npm run lint`, `npm run build`
- [ ] 브라우저 화면 확인 — 로그인 테스트 세션 없음
- [ ] "배치를 전반적으로 수정해달라"는 요청은 로그인 화면 확인 없이 코드로만 판단함. 코드 비교로 찾은 뚜렷한 불일치 외에는 임의로 변형하지 않았습니다.

## 주의사항

- 현재 사용자 API에는 이름과 이메일만 있으므로 생년월일·전화번호·경력·등급 통계는 실제 값을 추측하지 않습니다.
- AI 매칭 설정 API가 확인되지 않아 토글은 현재 화면 상태만 제공합니다.
- 비밀번호 변경은 클라이언트와 프리랜서가 공유하는 `MyPagePasswordChange`를 그대로 사용합니다.
- `ClientAccountCancellation`의 탈퇴 확인 입력창은 위험 동작 강조를 위해 포커스 시 빨간 테두리를 그대로 유지했고, 호버 남색 테두리는 적용하지 않았습니다.
- 관련 브랜치: 현재 브랜치 사용 중
- 작업 목적: 문의 목록·작성·상세·완료 화면의 핵심 사용자 흐름을 Jest와 React Testing Library로 검증합니다.

## 이번 작업 범위

- 문의 목록 필터·페이지·로딩·빈 상태·오류 테스트
- 문의 작성 입력·첨부파일·접수 성공·실패·정리 테스트
- 문의 상세 상태·첨부파일·권한 오류·재시도 테스트
- 문의 완료 화면 링크 테스트
- 전체 테스트·TypeScript·ESLint·빌드 검증

## 진행 상황

- [x] 관련 코드와 기존 테스트 형식 확인
- [x] fixture 및 Mock 구성
- [x] 목록·작성 테스트 작성
- [x] 상세·완료 테스트 작성
- [x] 전체 검증 및 문서 갱신

## 실행한 검증

- [x] 관련 테스트 — 4 suites, 18 tests 통과
- [x] 전체 테스트 — 12 suites, 55 tests 통과
- [x] `npx tsc --noEmit`
- [x] `npm run lint` — 오류 0건, 기존 coverage 산출물 경고 1건
- [x] `npm run build` — 최초 Google Fonts 네트워크 차단 실패 후 네트워크 허용 환경에서 통과
- [x] `git diff --check`

## 주의사항

- 서비스 모듈을 Mock 처리하며 실제 파일 및 문의 API는 호출하지 않습니다.
- 기존 `.ai/PM-발표노트.md` 변경은 사용자 작업이므로 수정하지 않습니다.
