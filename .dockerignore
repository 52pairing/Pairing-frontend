# 빌드 컨텍스트를 줄인다. node_modules 를 그대로 보내면 전송만 수십 초가 걸리고,
# 호스트(Windows)에서 설치한 바이너리가 alpine 이미지에 섞여 들어가 깨질 수 있다.
node_modules
.next
out
build
coverage

.git
.github
.gitignore

# 로컬 환경변수는 이미지에 들어가면 안 된다. 빌드 값은 build arg 로 넘긴다.
.env
.env.*

.claude
.ai
docs
outputs
tests
unit-tests
*.md
!README.md

.vscode
.idea
.DS_Store
npm-debug.log*
*.tsbuildinfo
